---
phase: 09.1-page-collections-and-index-pattern
plan: 03
subsystem: api
tags: [firebase-admin, firestore, server-components, nextjs]

# Dependency graph
requires:
  - phase: 09.1-01
    provides: wave-0 TDD stubs including server-side getCollectionPages test (source-scan and skip-guarded behavior tests)
provides:
  - getCollectionPages(parentSlug) exported from src/server/index.js using Admin SDK chained API
affects:
  - 09.1-05 (build verification)
  - consumers calling getCollectionPages from Next.js Server Components

# Tech tracking
tech-stack:
  added: []
  patterns:
    - Admin SDK chained query: getAdminFirestore().collection('pages').where().orderBy().get()
    - No-parameter server function: calls getAdminFirestore() internally (matches getCMSContent pattern)

key-files:
  created: []
  modified:
    - src/server/index.js

key-decisions:
  - "Admin SDK chained API used for getCollectionPages (not client SDK modular API) — consistent with getCMSContent pattern"
  - "No db parameter — calls getAdminFirestore() internally, matching getCMSContent signature convention"

patterns-established:
  - "Server entry exports use getAdminFirestore() internally — never accept a db parameter"

requirements-completed:
  - PAGE-COLL-02

# Metrics
duration: 3min
completed: 2026-04-14
---

# Phase 09.1 Plan 03: Server-Side getCollectionPages Summary

Admin SDK chained getCollectionPages(parentSlug) exported from src/server/index.js using db.collection().where().orderBy().get() pattern identical to getCMSContent

## Performance

- Duration: 3 min
- Started: 2026-04-14T04:43:45Z
- Completed: 2026-04-14T04:46:23Z
- Tasks: 1 (TDD)
- Files modified: 1

## Accomplishments
- Added getCollectionPages(parentSlug) to src/server/index.js using Admin SDK chained API
- Source-scan test 8 ("server/index.js uses Admin SDK chained collection().where().orderBy().get()") turned GREEN (was RED before)
- No regressions: full suite went from 28 failures to 27 failures (fixed exactly 1 test)
- All acceptance criteria verified with grep checks

## Task Commits

Each task was committed atomically:

1. Task 1: Export Admin-SDK getCollectionPages from src/server/index.js - `ea46e5a` (feat)

**Plan metadata:** (docs commit follows)

_Note: TDD task — source-scan test confirmed RED before implementation, GREEN after._

## Files Created/Modified
- `src/server/index.js` - Added getCollectionPages(parentSlug) export using Admin SDK chained API; 30 lines added (function + doc comment)

## Decisions Made
- Admin SDK chained API chosen (option b from RESEARCH open question 1) — consistent with getCMSContent which also uses db.doc().get() chained style
- No db parameter, same as getCMSContent — getAdminFirestore() called internally

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered

Tests 5-7 (getCollectionPages is exported, returns entries, returns empty array) remain SKIPPED. This is pre-existing and by design: the test file's import probe fails in the test environment because next/server cannot be resolved without .js extension in the Node ESM resolver. Tests 1-3 for getCMSContent have the same skip behavior. The STATE.md decision from Plan 01 documents: "Wave 0 tests use skip guards for import-probe tests; source-scan tests are unguarded to fail red until implementation ships." Test 8 (the source-scan test) correctly turned GREEN.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness
- getCollectionPages is now exported from jeeby-cms/server and callable from Next.js Server Components
- Plan 04 (CreatePageModal type selector) and Plan 05 (PageManager grouped view) are independent of this export
- Plan 05 (build verification) can confirm the export is bundled correctly after all implementation plans complete

## Self-Check: PASSED

- src/server/index.js: FOUND
- 09.1-03-SUMMARY.md: FOUND
- commit ea46e5a: FOUND

---
*Phase: 09.1-page-collections-and-index-pattern*
*Completed: 2026-04-14*
