---
phase: 06-block-editor
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - src/admin/PageManager.js
  - src/admin/PageManager.test.js
  - src/admin/PageEditor.test.js
  - src/admin/BlockCanvas.test.js
  - src/admin/AddBlockButton.test.js
  - src/admin/BlockTypePicker.test.js
  - src/admin/EditorHeader.test.js
  - src/admin/UndoToast.test.js
  - src/admin/editors/TitleEditor.test.js
  - src/admin/editors/TextEditor.test.js
  - src/admin/editors/ImageEditor.test.js
  - src/admin/editors/VideoEditor.test.js
  - src/admin/editors/GalleryEditor.test.js
autonomous: true
requirements:
  - EDIT-01
  - EDIT-02

must_haves:
  truths:
    - "Tiptap packages are installed and available for import"
    - "PageManager page names are links to /admin/pages/[slug]"
    - "PageManager has an Edit button in the Actions column"
    - "All 11 Phase 6 test files exist and run without syntax errors"
  artifacts:
    - path: "package.json"
      provides: "Tiptap devDependencies"
      contains: "@tiptap/react"
    - path: "src/admin/PageManager.js"
      provides: "Edit links to page editor"
      contains: "/admin/pages/"
    - path: "src/admin/PageEditor.test.js"
      provides: "Test scaffold for PageEditor"
      contains: "readFileSync"
    - path: "src/admin/editors/TitleEditor.test.js"
      provides: "Test scaffold for TitleEditor"
      contains: "readFileSync"
  key_links:
    - from: "src/admin/PageManager.js"
      to: "/admin/pages/[slug]"
      via: "anchor href"
      pattern: "href=.*admin/pages/"
---

<objective>
Install Tiptap dependencies, create all Phase 6 test scaffolds (Nyquist Wave 0), and wire PageManager edit navigation links.

Purpose: Establishes the foundation for all Phase 6 work -- dependencies available, test harnesses ready, and the entry point from PageManager to the editor is live.
Output: package.json with Tiptap deps, 11 test files, PageManager with edit links.
</objective>

<execution_context>
@/Users/joseph/.claude/get-shit-done/workflows/execute-plan.md
@/Users/joseph/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-block-editor/06-CONTEXT.md
@.planning/phases/06-block-editor/06-RESEARCH.md
@.planning/phases/06-block-editor/06-UI-SPEC.md

<interfaces>
<!-- Existing test pattern used by all prior phases -->
From src/admin/PageManager.test.js:
```javascript
import { test } from 'node:test'
import assert from 'node:assert/strict'
import { readFileSync } from 'node:fs'

const src = readFileSync(new URL('./PageManager.js', import.meta.url), 'utf8')
```

From src/firebase/firestore.js:
```javascript
export async function getPage(db, slug) // Returns page document data or null
export async function saveDraft(db, slug, blocks) // Updates draft.blocks + updatedAt
```

From src/blocks/Video.js:
```javascript
export function toEmbedUrl(url) // Converts YouTube/Vimeo/Loom URLs to embed src
```

Block field schemas (ground truth from source files):
- Title: data.level (h2-h6), data.text
- RichText: data.html
- Image: data.src, data.alt (NOT data.url)
- Video: data.url (or data.src fallback)
- Gallery: data.items array of { src, alt } (NOT { url, alt })
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install Tiptap dependencies and create test scaffold directory</name>
  <files>package.json, src/admin/editors/ (directory)</files>
  <read_first>
    - /Users/joseph/Documents/Web Dev/jeeby-cms/package.json
  </read_first>
  <action>
1. Run `npm install --save-dev @tiptap/react @tiptap/starter-kit` in the project root.
2. Verify `package.json` devDependencies now contains both `@tiptap/react` and `@tiptap/starter-kit`.
3. Create the `src/admin/editors/` directory (it does not yet exist).

IMPORTANT: Do NOT add @tiptap packages to the `external` array in `tsup.config.js` Entry 2 (admin). Tiptap must be BUNDLED into the admin output, not externalized. The current config already omits it from externals, which is correct. Do not touch tsup.config.js.
  </action>
  <verify>
    <automated>node -e "const p=require('./package.json'); const ok=p.devDependencies['@tiptap/react'] && p.devDependencies['@tiptap/starter-kit']; if(!ok) process.exit(1); console.log('Tiptap deps OK')"</automated>
  </verify>
  <acceptance_criteria>
    - package.json contains `"@tiptap/react"` in devDependencies
    - package.json contains `"@tiptap/starter-kit"` in devDependencies
    - Directory `src/admin/editors/` exists
    - tsup.config.js is NOT modified (Tiptap not in external array)
  </acceptance_criteria>
  <done>Tiptap packages installed as devDependencies; editors directory created; tsup config unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Create all 11 Phase 6 source-inspection test scaffolds</name>
  <files>
    src/admin/PageEditor.test.js,
    src/admin/BlockCanvas.test.js,
    src/admin/AddBlockButton.test.js,
    src/admin/BlockTypePicker.test.js,
    src/admin/EditorHeader.test.js,
    src/admin/UndoToast.test.js,
    src/admin/editors/TitleEditor.test.js,
    src/admin/editors/TextEditor.test.js,
    src/admin/editors/ImageEditor.test.js,
    src/admin/editors/VideoEditor.test.js,
    src/admin/editors/GalleryEditor.test.js
  </files>
  <read_first>
    - /Users/joseph/Documents/Web Dev/jeeby-cms/src/admin/PageManager.test.js (pattern reference)
    - /Users/joseph/Documents/Web Dev/jeeby-cms/.planning/phases/06-block-editor/06-RESEARCH.md (test map at line 574-616)
  </read_first>
  <action>
Create 11 test files using the established source-inspection pattern (readFileSync + string assertions). Each file reads its corresponding source file and asserts structural/accessibility contracts. The source files do NOT exist yet -- tests will fail until Plans 02-04 create them. This is intentional (Nyquist scaffolding).

All tests follow this exact pattern:
```javascript
import { test } from 'node:test'
import assert from 'node:assert/strict'
import { readFileSync } from 'node:fs'
const src = readFileSync(new URL('./{ComponentName}.js', import.meta.url), 'utf8')
```

Test assertions per file (from RESEARCH.md test map):

**src/admin/PageEditor.test.js** — reads PageEditor.js AND index.js:
- PageEditor exports from src/admin/index.js: `adminSrc.includes('PageEditor')`
- PageEditor imports getPage from firestore: `src.includes('getPage')`
- PageEditor imports saveDraft from firestore: `src.includes('saveDraft')`
- PageEditor uses useCMSFirebase: `src.includes('useCMSFirebase')`
- PageEditor uses crypto.randomUUID for block IDs: `src.includes('crypto.randomUUID')`
- PageEditor has "use client" directive: `src.trimStart().startsWith('"use client"')`
- PageEditor uses debounce pattern: `src.includes('clearTimeout') && src.includes('useRef')`
- Delete undo uses 5000ms timer: `src.includes('5000')`

**src/admin/BlockCanvas.test.js** — reads BlockCanvas.js:
- Uses Reorder.Group: `src.includes('Reorder.Group')`
- Reorder.Group has aria-label: `src.includes('aria-label="Page blocks"')`
- Uses Reorder.Item: `src.includes('Reorder.Item')`
- dragListener={false}: `src.includes('dragListener={false}')`
- Uses useDragControls: `src.includes('useDragControls')`
- BlockCard uses article element: `src.includes('<article')`
- Article has aria-label with block type: `/aria-label=.*block/.test(src)`
- Drag handle has aria-hidden: `src.includes('aria-hidden="true"')`
- Delete button has aria-label with Delete: `/aria-label=.*Delete/.test(src)`
- Delete button minHeight 44px: `src.includes("minHeight: '44px'") || src.includes('minHeight: 44')`
- "use client" directive

**src/admin/AddBlockButton.test.js** — reads AddBlockButton.js:
- Has aria-label="Add block": `src.includes('aria-label="Add block"')`
- Has aria-expanded: `src.includes('aria-expanded')`
- Has aria-haspopup="listbox": `src.includes('aria-haspopup="listbox"')`
- Button type="button": `src.includes('type="button"')`
- "use client" directive

**src/admin/BlockTypePicker.test.js** — reads BlockTypePicker.js:
- Has role="listbox": `src.includes('role="listbox"')`
- Has aria-label="Choose block type": `src.includes('aria-label="Choose block type"')`
- Lists all 5 display names: Title, Text, Image, Video, Gallery
- Has role="option": `src.includes('role="option"')`
- Handles Escape key: `src.includes('Escape')`
- Handles Enter key: `src.includes('Enter')`
- Handles ArrowDown: `src.includes('ArrowDown')`

**src/admin/EditorHeader.test.js** — reads EditorHeader.js:
- Has h1 element: `src.includes('<h1')`
- Has back link: `src.includes('Back to Pages') || src.includes('aria-label="Back to Pages"')`
- Save status has role="status": `src.includes('role="status"')`
- Save status has aria-live="polite": `src.includes('aria-live="polite"')`
- Save status has aria-atomic="true": `src.includes('aria-atomic="true"')`
- Contains "Saving...": `src.includes('Saving...')`
- Contains "Saved": `src.includes('Saved')`
- Contains "Save failed": `src.includes('Save failed')`
- "use client" directive

**src/admin/UndoToast.test.js** — reads UndoToast.js:
- Has role="status": `src.includes('role="status"')`
- Has aria-live="polite": `src.includes('aria-live="polite"')`
- Has aria-atomic="true": `src.includes('aria-atomic="true"')`
- Undo button text: `src.includes('Undo delete')`
- Position fixed bottom: `src.includes('position: ') && src.includes('fixed')`
- "use client" directive

**src/admin/editors/TitleEditor.test.js** — reads TitleEditor.js:
- Uses contentEditable: `src.includes('contentEditable')`
- Has role="textbox": `src.includes('role="textbox"')`
- Has aria-label="Title text": `src.includes('aria-label="Title text"')`
- Has heading level select: `src.includes('<select')`
- Select has aria-label="Heading level": `src.includes('aria-label="Heading level"')`
- Has h2-h6 options: `src.includes("'h2'") && src.includes("'h6'")`
- Writes data.text: `src.includes('data.text') || src.includes('text:')`
- Writes data.level: `src.includes('data.level') || src.includes('level:')`
- Font sizes for canvas fidelity: `src.includes('28px')` (h2 size from UI-SPEC)

**src/admin/editors/TextEditor.test.js** — reads TextEditor.js:
- Imports from @tiptap/react: `src.includes('@tiptap/react')`
- Imports StarterKit: `src.includes('StarterKit') || src.includes('@tiptap/starter-kit')`
- Uses useEditor: `src.includes('useEditor')`
- Uses EditorContent: `src.includes('EditorContent')`
- Writes data.html: `src.includes('data.html') || src.includes('.html') || src.includes('getHTML')`
- Has aria-label="Text content": `src.includes('aria-label="Text content"')`
- "use client" directive

**src/admin/editors/ImageEditor.test.js** — reads ImageEditor.js:
- Writes data.src (NOT data.url): `src.includes('data.src') || (src.includes("src:") && !src.includes("url:"))`
- Alt text input: `src.includes('aria-label="Alt text"') || src.includes('Alt text')`
- Image URL input: `src.includes('type="url"') || src.includes('Image URL')`
- Alt hint text: `src.includes('Describe the image for screen readers')`
- Shows img preview: `src.includes('<img')`
- "use client" directive

**src/admin/editors/VideoEditor.test.js** — reads VideoEditor.js:
- Imports or uses toEmbedUrl: `src.includes('toEmbedUrl')`
- Has iframe preview: `src.includes('<iframe') || src.includes('iframe')`
- Video URL input: `src.includes('Video URL')`
- Video URL hint: `src.includes('YouTube, Vimeo, or Loom')`
- Invalid URL error text: `src.includes('Unrecognised video URL')`
- "use client" directive

**src/admin/editors/GalleryEditor.test.js** — reads GalleryEditor.js:
- Writes item.src (NOT item.url): `src.includes('item.src') || src.includes("src:")`
- Items list: `src.includes('aria-label="Gallery images"')`
- Add image button: `src.includes('Add image')`
- Remove button: `src.includes('Remove gallery image')`
- Alt text per item: `src.includes('Alt text for item')`
- "use client" directive
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const files=['src/admin/PageEditor.test.js','src/admin/BlockCanvas.test.js','src/admin/AddBlockButton.test.js','src/admin/BlockTypePicker.test.js','src/admin/EditorHeader.test.js','src/admin/UndoToast.test.js','src/admin/editors/TitleEditor.test.js','src/admin/editors/TextEditor.test.js','src/admin/editors/ImageEditor.test.js','src/admin/editors/VideoEditor.test.js','src/admin/editors/GalleryEditor.test.js']; const missing=files.filter(f=>!fs.existsSync(f)); if(missing.length){console.error('Missing:',missing);process.exit(1)} console.log('All 11 test files exist')"</automated>
  </verify>
  <acceptance_criteria>
    - All 11 test files exist at the paths listed above
    - Each test file imports from `node:test`, `node:assert/strict`, `node:fs`
    - Each test file uses `readFileSync(new URL('./{SourceFile}.js', import.meta.url), 'utf8')`
    - PageEditor.test.js asserts: getPage, saveDraft, useCMSFirebase, crypto.randomUUID, clearTimeout, useRef, 5000
    - BlockCanvas.test.js asserts: Reorder.Group, Reorder.Item, dragListener={false}, useDragControls, article, aria-hidden="true"
    - AddBlockButton.test.js asserts: aria-label="Add block", aria-expanded, aria-haspopup="listbox"
    - BlockTypePicker.test.js asserts: role="listbox", role="option", Title, Text, Image, Video, Gallery
    - EditorHeader.test.js asserts: h1, role="status", aria-live="polite", Saving..., Saved, Save failed
    - UndoToast.test.js asserts: role="status", Undo delete, position fixed
    - TitleEditor.test.js asserts: contentEditable, role="textbox", select, Heading level
    - TextEditor.test.js asserts: @tiptap/react, useEditor, EditorContent, getHTML
    - ImageEditor.test.js asserts: data.src or src:, Alt text, img preview
    - VideoEditor.test.js asserts: toEmbedUrl, iframe, Unrecognised video URL
    - GalleryEditor.test.js asserts: item.src or src:, Gallery images, Add image, Remove gallery image
  </acceptance_criteria>
  <done>All 11 source-inspection test scaffolds created; each will fail until corresponding source files are built in Plans 02-04.</done>
</task>

<task type="auto">
  <name>Task 3: Wire PageManager edit navigation (page name link + Edit button)</name>
  <files>src/admin/PageManager.js, src/admin/PageManager.test.js</files>
  <read_first>
    - /Users/joseph/Documents/Web Dev/jeeby-cms/src/admin/PageManager.js
    - /Users/joseph/Documents/Web Dev/jeeby-cms/src/admin/PageManager.test.js
  </read_first>
  <action>
Modify `src/admin/PageManager.js` to add two navigation access points to the page editor:

1. **Page name becomes a link:** In the Name cell (line ~294), replace the plain `<span>{page.name || page.slug}</span>` with `<a href={'/admin/pages/' + encodeURIComponent(page.slug)} style={{color: '#2563EB', textDecoration: 'none', fontSize: '14px'}}>{page.name || page.slug}</a>`. Keep the existing Edit button for inline rename adjacent to the link.

2. **Edit button in Actions column:** In the Actions `<td>` (line ~357), add an Edit link BEFORE the existing Delete button:
```jsx
<a
  href={'/admin/pages/' + encodeURIComponent(page.slug)}
  aria-label={'Edit blocks for ' + page.slug}
  style={{
    color: '#2563EB', background: 'none', border: 'none',
    cursor: 'pointer', fontSize: '14px', minHeight: '44px', padding: '8px 16px',
    textDecoration: 'none', display: 'inline-block', lineHeight: '44px'
  }}
>Edit</a>
```

3. **Extend PageManager.test.js** with two new tests:
```javascript
test('PageManager page name is a link to /admin/pages/[slug]', () => {
  assert.ok(src.includes('/admin/pages/'), 'Page name must link to /admin/pages/[slug]')
  assert.ok(src.includes('<a'), 'Page name must be an anchor element')
})

test('PageManager has Edit button in Actions column linking to editor', () => {
  assert.ok(/aria-label=.*Edit blocks/.test(src), 'Edit button must have aria-label describing action')
})
```
  </action>
  <verify>
    <automated>cd "/Users/joseph/Documents/Web Dev/jeeby-cms" && node --import ./scripts/test-register.js --experimental-test-module-mocks --test 'src/admin/PageManager.test.js'</automated>
  </verify>
  <acceptance_criteria>
    - PageManager.js contains `href={'/admin/pages/' + encodeURIComponent(page.slug)}`
    - PageManager.js contains `aria-label={'Edit blocks for ' + page.slug}`
    - PageManager.js contains at least two `<a` elements per page row (page name link + edit link)
    - PageManager.test.js contains test for `/admin/pages/` link
    - PageManager.test.js contains test for `Edit blocks` aria-label
    - All PageManager.test.js tests pass (including existing tests)
  </acceptance_criteria>
  <done>PageManager page names are clickable links to /admin/pages/[slug]; Actions column has Edit link; all tests pass.</done>
</task>

</tasks>

<verification>
1. `node -e "const p=require('./package.json'); console.log(p.devDependencies['@tiptap/react'], p.devDependencies['@tiptap/starter-kit'])"` prints two version strings
2. `ls src/admin/editors/*.test.js | wc -l` prints 5
3. `ls src/admin/*.test.js | wc -l` prints at least 10 (existing 5 + new 6)
4. `node --import ./scripts/test-register.js --experimental-test-module-mocks --test 'src/admin/PageManager.test.js'` passes
</verification>

<success_criteria>
- Tiptap devDependencies installed; tsup.config.js unchanged
- All 11 Phase 6 test files exist and parse without syntax errors
- PageManager links to /admin/pages/[slug] via page name and Edit button
- Existing PageManager tests still pass
</success_criteria>

<output>
After completion, create `.planning/phases/06-block-editor/06-01-SUMMARY.md`
</output>
