---
phase: 02-firebase-layer
plan: "01"
type: execute
wave: 0
depends_on: []
files_modified:
  - src/firebase/init.test.js
  - src/firebase/firestore.test.js
  - src/firebase/auth.test.js
  - src/firebase/storage.test.js
autonomous: true
requirements:
  - FIRE-01
  - FIRE-02
  - FIRE-03
  - FIRE-04

must_haves:
  truths:
    - "All four test files exist and each passes without error"
    - "`node --test src/firebase/*.test.js` exits 0"
  artifacts:
    - path: "src/firebase/init.test.js"
      provides: "Unit test scaffold for multi-init guard and CMSProvider"
    - path: "src/firebase/firestore.test.js"
      provides: "Unit test scaffold for getPage null-return and CRUD mocks"
    - path: "src/firebase/auth.test.js"
      provides: "Unit test scaffold for useAuth loading transitions"
    - path: "src/firebase/storage.test.js"
      provides: "Unit test scaffold for uploadFile progress callback and deleteFile"
  key_links:
    - from: "src/firebase/init.test.js"
      to: "src/firebase/init.js"
      via: "import of initFirebase, getFirebaseInstances"
      pattern: "initFirebase|getFirebaseInstances"
    - from: "src/firebase/firestore.test.js"
      to: "src/firebase/firestore.js"
      via: "import of getPage, savePage, saveDraft, publishPage, deletePage"
      pattern: "getPage|savePage|publishPage|deletePage"
    - from: "src/firebase/storage.test.js"
      to: "src/firebase/storage.js"
      via: "import of uploadFile, deleteFile"
      pattern: "uploadFile|deleteFile"
---

<objective>
Create all Wave 0 test stubs for the Firebase layer. Each stub imports from the source file that Plan 02–05 will implement, calls the functions with mocked Firebase objects, and asserts the expected behaviors. Tests must use Node.js built-in test runner (no npm install required) and pass immediately — the source files do not exist yet, so stubs import from non-existent paths but this is acceptable because Wave 0 tests use `try/catch` skips or `mock.module` to avoid failures when implementation is missing.

Purpose: Establish the verification harness before any implementation, enforcing the Nyquist rule that every task has a runnable automated verify command.
Output: Four test files in `src/firebase/`
</objective>

<execution_context>
@/Users/joseph/.claude/get-shit-done/workflows/execute-plan.md
@/Users/joseph/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-firebase-layer/02-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Firebase init and CMSProvider test stubs</name>
  <files>src/firebase/init.test.js</files>
  <action>
Create `src/firebase/` directory and `src/firebase/init.test.js` using Node.js built-in test runner (`node:test` and `node:assert`).

The test file must cover FIRE-01 behaviors using mocks — do NOT import the real Firebase SDK:

```javascript
import { test, mock } from 'node:test'
import assert from 'node:assert/strict'
```

Mock `firebase/app` using `mock.module` before importing the source module. The tests must:

1. **Multi-init guard** — when `getApps()` returns a non-empty array, `initFirebase()` must NOT call `initializeApp()` again. Verify by counting `initializeApp` call count.
2. **First-time init** — when `getApps()` returns an empty array, `initFirebase()` calls `initializeApp()` exactly once.
3. **`getFirebaseInstances()` throws when not initialized** — calling `getFirebaseInstances()` before `initFirebase()` throws an Error with message containing "Firebase not initialized".
4. **`initFirebase()` returns `{ app, db, auth, storage }` shape** — all four keys present.

Since `src/firebase/init.js` does not exist yet, wrap imports in `try/catch` and `test.skip` if the module is not found. Use this pattern for all stubs:

```javascript
let initFirebase, getFirebaseInstances
try {
  const mod = await import('./init.js')
  initFirebase = mod.initFirebase
  getFirebaseInstances = mod.getFirebaseInstances
} catch {
  // implementation not yet written — tests will be skipped
}

test('multi-init guard: does not call initializeApp twice', { skip: !initFirebase }, async (t) => {
  // test body
})
```

Use `mock.module` to stub `firebase/app`, `firebase/firestore`, `firebase/auth`, and `firebase/storage` so no real Firebase calls are made.
  </action>
  <verify>
    <automated>node --test src/firebase/init.test.js 2>&1; echo "exit: $?"</automated>
  </verify>
  <done>File exists. `node --test src/firebase/init.test.js` exits 0 (tests either pass or are skipped due to missing implementation).</done>
</task>

<task type="auto">
  <name>Task 2: Create Firestore, Auth, and Storage test stubs</name>
  <files>src/firebase/firestore.test.js, src/firebase/auth.test.js, src/firebase/storage.test.js</files>
  <action>
Create three test stub files using the same pattern as Task 1 — import with try/catch, skip if source not found, mock Firebase SDK modules.

**`src/firebase/firestore.test.js`** (covers FIRE-02):
- `getPage()` returns `null` when Firestore `getDoc` returns a snapshot where `exists()` is false
- `savePage()` calls `setDoc` with merge option true
- `saveDraft()` calls `updateDoc` with `'draft.blocks'` key
- `publishPage()` copies `draft.blocks` to `published.blocks` via `updateDoc`
- `deletePage()` calls `deleteDoc`

Mock: `firebase/firestore` — stub `doc`, `getDoc`, `setDoc`, `updateDoc`, `deleteDoc`, `serverTimestamp` with simple spy functions.

**`src/firebase/auth.test.js`** (covers FIRE-03):
- `signIn()` calls `signInWithEmailAndPassword` with the passed email and password and returns `result.user`
- `signOut()` calls `firebaseSignOut`
- `subscribeToAuthState()` calls `onAuthStateChanged` and returns the unsubscribe function it returns

Mock: `firebase/auth` — stub all named imports with spy functions.

**`src/firebase/storage.test.js`** (covers FIRE-04):
- `uploadFile()` calls `onProgress` callback during upload with a percent value (0–100)
- `uploadFile()` resolves with a string URL (from mocked `getDownloadURL`)
- `deleteFile()` calls `deleteObject` with the correct storage ref

Mock: `firebase/storage` — stub `ref`, `uploadBytesResumable` (return a fake task that immediately fires 'state_changed' with bytesTransferred=50, totalBytes=100 then calls complete handler), `getDownloadURL` (resolves 'https://example.com/file'), `deleteObject`.

All three files must use the skip-on-missing-import pattern. No watch flags.
  </action>
  <verify>
    <automated>node --test src/firebase/firestore.test.js src/firebase/auth.test.js src/firebase/storage.test.js 2>&1; echo "exit: $?"</automated>
  </verify>
  <done>All three files exist. `node --test src/firebase/*.test.js` exits 0.</done>
</task>

</tasks>

<verification>
`node --test src/firebase/*.test.js` exits 0 — all tests pass or are skipped with "skip: !fn" due to missing implementations.
</verification>

<success_criteria>
- `src/firebase/init.test.js`, `src/firebase/firestore.test.js`, `src/firebase/auth.test.js`, `src/firebase/storage.test.js` all exist
- `node --test src/firebase/*.test.js` exits 0
- Each test file covers the behaviors listed in `02-VALIDATION.md`
- No real Firebase SDK calls are made — all Firebase imports are mocked
</success_criteria>

<output>
After completion, create `.planning/phases/02-firebase-layer/02-01-SUMMARY.md`
</output>
