---
phase: 11-i18n-localization-for-admin-panel-and-block-components
task: in-progress (admin UI string pass — not a formal GSD plan)
status: in_progress
last_updated: 2026-04-14T23:50:45.743Z
---

<current_state>
Mid-session admin UI localization pass. ADMIN_STRINGS expanded from 21 to ~100 keys.
useT()/tf()/BLOCK_DISPLAY_KEYS wired into 14/15 components. 23 new test failures introduced
because useT() calls useCMSFirebase() which throws when components render without CMSProvider
in tests. Fix is clear. PageManager.js not yet touched.
</current_state>

<completed_work>

- src/index.js — added uiLocale (browser-detected, SSR-safe) to CMSProvider context
- src/admin/i18n.js — ADMIN_STRINGS expanded to ~100 keys EN+FR (auth, nav, editor header, all modals, toasts, block picker, page manager keys all defined)
- src/admin/useT.js — NEW FILE: exports useT(), tf(), BLOCK_DISPLAY_KEYS (split from i18n.js to keep i18n.test.js import-free)
- 14 components wired with useT():
  - LoginPage.js — email, password, show/hide, signIn, invalidCredentials
  - AdminNav.js — signOut, adminNavLabel
  - EditorHeader.js — all doc status labels, back nav, title/slug edit aria-labels, publish button, settings, copy URL, view page
  - CreatePageModal.js — heading, all field labels, options, buttons, error messages
  - DeletePageModal.js — heading, body (interpolated slug), buttons
  - PublishConfirmModal.js — title (interpolated name), body, error, buttons
  - PublishToast.js — pagePublished
  - UndoToast.js — blockDeleted (interpolated), undoDeleteAriaLabel (interpolated), BLOCK_DISPLAY_KEYS
  - UnsavedChangesWarning.js — title, body, buttons
  - SignOutModal.js — heading, body (interpolated pageName), error, buttons
  - PageMetaModal.js — heading, labels, hints, image alt, aria-labels, buttons
  - BlockTypePicker.js — chooseBlockType aria-label, all BLOCK_DEFS now use labelKey/hintKey
  - BlockGutter.js — dragToReorder/deleteBlockAriaLabel via tf() + BLOCK_DISPLAY_KEYS
  - AddBlockButton.js — addBlock aria-label
</completed_work>

<remaining_work>

1. FIX 23 NEW TEST FAILURES (highest priority):
   - Tests render components without CMSProvider → useT() → useCMSFirebase() throws
   - Fix: add `uiLocale: 'en'` to mock context in each affected test file
   - Affected test files: AddBlockButton.test.js, BlockGutter (no test?), BlockTypePicker.test.js,
     CreatePageModal.test.js, DeletePageModal.test.js + others
   - Pattern: find where tests mock useCMSFirebase context and add uiLocale: 'en' to the mock value
   - Run: node --test --test-reporter=tap 2>&1 | grep "^not ok" to see full list

2. WIRE PageManager.js (large file, 1300+ lines):
   - Add: import { useT, tf } from './useT.js'
   - Add uiLocale to mock context in PageManager.test.js
   - Convert SORT_OPTIONS: replace label/hint props with labelKey/hintKey
   - Convert STATUS_PROPS: replace label with labelKey, use t(STATUS_PROPS[s].labelKey) at render
   - Add const t = useT() in both PageManager and SortPicker function components
   - Replace all hardcoded strings (table headers, empty states, error states, pagination, etc.)
   - Keys already defined in ADMIN_STRINGS: sortRecent/Alpha, filterDrafts/Changes/Published,
     sortRecentHint/sortAlphaHint etc., colName/Slug/Status/LastPublished/Actions,
     noPagesYet/noPagesEmptyBody/failedToLoadPages, newPage/createFirstPage/uploadMedia,
     searchPagesPlaceholder/Label, clearSearch/clearFilter, dismiss, collections,
     statusPublished/Draft/Changes, notYet, editBlocksFor, paginationLabel, previousPage/nextPage
</remaining_work>

<decisions_made>

- Split useT/tf/BLOCK_DISPLAY_KEYS into src/admin/useT.js (separate from i18n.js)
  so i18n.test.js can import ADMIN_STRINGS without pulling in React/index.js JSX
- uiLocale detected once in CMSProvider via navigator.language — SSR-safe, lazy useState init
- BLOCK_DEFS in BlockTypePicker now uses labelKey/hintKey instead of label/hint
  so picker options are fully translatable
- EditorHeader date formatting functions take t() as argument (not a hook inside helper)
</decisions_made>

<blockers>

- 23 new test failures: components throw when rendered without CMSProvider because useT() calls useCMSFirebase()
  Workaround: add uiLocale: 'en' to all mock context objects in test files
  NOT blocking for runtime — only affects test suite
</blockers>

<context>
This is an ad-hoc pass outside the formal GSD plan structure (Phase 11 is already "complete").
The user asked to "implement the admin UI localization pass" based on context from a prior session
that had inventoried ~80 new string keys needed across 15 components.

Architecture chosen:
- uiLocale in CMSProvider (browser language, not content locale)
- useT() hook returns t(key) lookup function
- tf(str, vars) interpolates {{variable}} placeholders
- BLOCK_DISPLAY_KEYS maps block type → ADMIN_STRINGS key

All 100 keys (EN + FR) are already written and in i18n.js.
The i18n.test.js key-parity test passes (2/2).
Only remaining work is fixing test mocks and wiring PageManager.
</context>

<next_action>
Start fresh session. Run:
  node --test --test-reporter=tap 2>&1 | grep "^not ok"
to see all 23 failing tests. Then grep each test file for the mock context pattern and add uiLocale: 'en'.
After tests green, tackle PageManager.js.
</next_action>
