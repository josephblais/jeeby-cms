---
phase: 08-css-theming
plan: 02
type: execute
wave: 2
depends_on:
  - 08-01
files_modified:
  - src/admin/index.js
  - src/admin/AdminNav.js
  - src/admin/LoginPage.js
  - src/admin/PageManager.js
  - src/admin/EditorHeader.js
  - src/admin/PageEditor.js
  - src/admin/BlockCanvas.js
autonomous: true
requirements:
  - CSS-01
  - CSS-03
must_haves:
  truths:
    - "Admin components reference CSS classes from styles/cms.css instead of inline style objects for visual properties"
    - "Accessibility-critical inline styles remain in place (live regions, runtime-conditional cursor/opacity/pointerEvents)"
    - "The @keyframes jeeby-spin <style> tag is removed from index.js (moved to CSS in Plan 01)"
    - "No hardcoded hex color values remain in migrated components"
  artifacts:
    - path: "src/admin/index.js"
      provides: "Admin root with inline styles removed, no <style> tag"
    - path: "src/admin/AdminNav.js"
      provides: "Nav with all visual inline styles removed"
    - path: "src/admin/LoginPage.js"
      provides: "Login page with form styles from CSS"
    - path: "src/admin/PageManager.js"
      provides: "Page manager with table/layout styles from CSS"
    - path: "src/admin/EditorHeader.js"
      provides: "Editor header with layout styles from CSS"
    - path: "src/admin/BlockCanvas.js"
      provides: "Block canvas with new jeeby-cms-drag-handle class"
  key_links:
    - from: "src/admin/index.js"
      to: "styles/cms.css"
      via: "className references matching CSS selectors"
      pattern: "jeeby-cms-admin"
---

<objective>
Migrate inline styles from core admin layout components to CSS classes defined in Plan 01's styles/cms.css. This covers the main shell (index.js, AdminNav), authentication (LoginPage), page management (PageManager), and editor layout (EditorHeader, PageEditor, BlockCanvas).

Purpose: Components currently use inline `style={{}}` objects for visual properties that should come from the CSS file. Migration enables consumer theming via CSS var overrides and eliminates hardcoded colors.

Output: 7 admin component files with inline visual styles removed, referencing CSS classes instead.
</objective>

<execution_context>
@/Users/joseph/.claude/get-shit-done/workflows/execute-plan.md
@/Users/joseph/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/08-css-theming/08-CONTEXT.md
@.planning/phases/08-css-theming/08-RESEARCH.md
@.planning/phases/08-css-theming/08-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate inline styles from index.js, AdminNav.js, LoginPage.js, PageManager.js</name>
  <files>src/admin/index.js, src/admin/AdminNav.js, src/admin/LoginPage.js, src/admin/PageManager.js</files>
  <read_first>
    - src/admin/index.js
    - src/admin/AdminNav.js
    - src/admin/LoginPage.js
    - src/admin/PageManager.js
    - styles/cms.css (to verify class names match)
    - .planning/phases/08-css-theming/08-RESEARCH.md (inline style migration map -- "Migrate to CSS" and "Keep Inline" tables)
  </read_first>
  <action>
**src/admin/index.js** -- Make these exact changes:

1. REMOVE the `<style>` tag: delete `<style>{`@keyframes jeeby-spin { to { transform: rotate(360deg) } }`}</style>` (line 22). The @keyframes rule now lives in styles/cms.css.

2. Loading state `.jeeby-cms-admin` div (line 13-14): Remove `style={{ minHeight: '100vh', display: 'flex', alignItems: 'center', justifyContent: 'center' }}`. The `.jeeby-cms-admin` CSS rule provides `min-height: 100vh`. Add a wrapper div with `className="jeeby-cms-loading"` around the spinner content to get the centering (loading class has display flex + center).

3. Spinner div (line 17-19): Remove `style={{ display: 'inline-block', width: '32px', height: '32px', animation: 'jeeby-spin 0.75s linear infinite' }}`. The `.jeeby-cms-spinner` class provides all of this.

4. Login state `.jeeby-cms-admin` div (line 29-31): Remove `style={{ minHeight: '100vh', display: 'flex', alignItems: 'center', justifyContent: 'center' }}`. KEEP `display: flex, alignItems: center, justifyContent: center` as inline since this is a conditional render branch for centering the login card. The `min-height: 100vh` comes from `.jeeby-cms-admin` CSS. So change to: `style={{ display: 'flex', alignItems: 'center', justifyContent: 'center' }}`.

5. Authenticated state `.jeeby-cms-admin` div (line 38): Remove `style={{ minHeight: '100vh' }}`. Provided by `.jeeby-cms-admin` CSS.

6. Skip link (line 39-41): Remove `style={{ position: 'absolute', left: '-9999px', top: '0', zIndex: 1000 }}`. The `.jeeby-cms-skip-link` CSS handles this.

**src/admin/AdminNav.js** -- Make these exact changes:

1. Header element (line 5-8): Remove `style={{ display: 'flex', alignItems: 'center', justifyContent: 'space-between', height: '56px' }}`. The `.jeeby-cms-nav` CSS provides all of this.

2. Sign out button (line 15-18): Remove `style={{ background: 'none', border: 'none', cursor: 'pointer', minHeight: '44px' }}`. The `.jeeby-cms-btn-ghost` CSS provides all of this.

**src/admin/LoginPage.js** -- Make these exact changes:

1. Login card div (line 30-32): Remove `style={{ maxWidth: '400px', minWidth: '320px', width: '100%' }}`. The `.jeeby-cms-login-card` CSS provides this.

2. Both label elements (lines 36, 48): Remove `style={{ display: 'block' }}`. The `.jeeby-cms-field label` CSS sets `display: block`.

3. Both input elements (lines 38-44, 50-56): Remove `style={{ display: 'block', width: '100%', boxSizing: 'border-box' }}`. The `.jeeby-cms-field input` CSS provides this.

4. Submit button (lines 59-67): Remove `style={{ display: 'block', width: '100%', minHeight: '44px', cursor: submitting ? 'not-allowed' : 'pointer' }}`. Add CSS-provided properties via class. KEEP the runtime-conditional cursor inline: `style={{ cursor: submitting ? 'not-allowed' : 'pointer', display: 'block', width: '100%' }}`. The `min-height: 44px` comes from `.jeeby-cms-btn-primary` CSS. Keep `display: block, width: 100%` inline since btn-primary is inline-flex by default and the login button needs to be full-width block.

**src/admin/PageManager.js** -- Make these exact changes:

1. All 3 `.jeeby-cms-live-region` divs (lines 172-177, 195-200, 221-226): KEEP inline styles. These are accessibility-critical sr-only patterns (`position: absolute, width: 1px, height: 1px, overflow: hidden, clip: rect(0,0,0,0), whiteSpace: nowrap`). Do NOT migrate.

2. Page list header div (line 229-231): Remove `style={{ display: 'flex', justifyContent: 'space-between', alignItems: 'center' }}`. The `.jeeby-cms-page-list-header` CSS provides this.

3. Both "New Page" buttons (lines 236-239 and 207-209): Remove `style={{ minHeight: '44px', cursor: 'pointer' }}`. The `.jeeby-cms-btn-primary` CSS provides `min-height: 44px` and `cursor: pointer`.

4. Table element (line 242): Remove `style={{ width: '100%', borderCollapse: 'collapse' }}`. The `.jeeby-cms-pages-table` CSS provides this.

5. Both inline-edit-input elements: Remove `style={{ display: 'block', width: '100%', boxSizing: 'border-box' }}`. The `.jeeby-cms-inline-edit-input` CSS provides this.

6. Name cell display span (line 273): Remove `style={{ display: 'flex', alignItems: 'center', gap: '8px' }}`. This is layout -- add a utility approach: keep it inline (it's a flex row for name + edit button, specific to this row).

7. Edit trigger buttons (lines 280-287, 316-324): KEEP `style={{ background: 'none', border: 'none', cursor: 'pointer', minHeight: '44px', opacity: 0 }}`. These have runtime opacity changes via onFocus/onBlur. The opacity: 0 default + JS toggling is the interaction pattern. Add `className="jeeby-cms-btn-ghost"` to get base button styles, but KEEP the inline `style={{ opacity: 0 }}` since it's dynamically overridden.

8. Actions cell Edit link (line 337-343): Remove `style={{ display: 'inline-block', minHeight: '44px', cursor: 'pointer' }}`. Add `style={{ minHeight: '44px', display: 'inline-flex', alignItems: 'center' }}` to maintain touch target. The link gets its color from `.jeeby-cms-pages-table a` CSS.

9. Actions cell Delete button (lines 346-351): Remove `style={{ background: 'none', border: 'none', cursor: 'pointer', minHeight: '44px' }}`. Add `className="jeeby-cms-btn-ghost"` (class provides these).

10. Loading spinner div in loading state (lines 181-184): Remove `style={{ width: '32px', height: '32px', animation: 'jeeby-spin 0.75s linear infinite' }}`. Add `className="jeeby-cms-spinner"`.

After all changes, verify: `grep -c "style={{" src/admin/PageManager.js` should return a LOWER count than before (live regions and the few runtime styles remain).
  </action>
  <verify>
    <automated>node --import ./scripts/test-register.js --experimental-test-module-mocks --test 'src/admin/index.js' 'src/admin/LoginPage.test.js' 'src/admin/PageManager.test.js' 'src/admin/AdminPanel.test.js' 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - src/admin/index.js does NOT contain `@keyframes jeeby-spin`
    - src/admin/index.js does NOT contain `<style>` tag
    - src/admin/AdminNav.js does NOT contain `style={{`
    - src/admin/LoginPage.js: `grep -c "style={{" src/admin/LoginPage.js` returns 1 (only the submit button cursor conditional)
    - src/admin/PageManager.js does NOT contain `borderCollapse`
    - src/admin/PageManager.js does NOT contain `width: '100%', borderCollapse`
    - src/admin/PageManager.js contains `className="jeeby-cms-spinner"` in the loading state
    - Existing tests pass: `node --import ./scripts/test-register.js --experimental-test-module-mocks --test 'src/admin/LoginPage.test.js'` exits 0
  </acceptance_criteria>
  <done>index.js has no style tag or spinner inline styles. AdminNav has no inline styles. LoginPage has only the runtime cursor conditional inline. PageManager has live-region sr-only styles inline (intentional) and no visual inline styles. All existing tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate inline styles from EditorHeader.js, PageEditor.js, BlockCanvas.js</name>
  <files>src/admin/EditorHeader.js, src/admin/PageEditor.js, src/admin/BlockCanvas.js</files>
  <read_first>
    - src/admin/EditorHeader.js
    - src/admin/PageEditor.js
    - src/admin/BlockCanvas.js
    - styles/cms.css (to verify class names match)
    - .planning/phases/08-css-theming/08-RESEARCH.md (inline style migration map)
  </read_first>
  <action>
**src/admin/EditorHeader.js** -- Make these exact changes:

1. Header element (line 37-39): Remove `style={{ display: 'flex', alignItems: 'center', justifyContent: 'space-between' }}`. The `.jeeby-cms-editor-header` CSS provides this.

2. Back link (line 44): Remove `style={{ minHeight: '44px', display: 'inline-flex', alignItems: 'center' }}`. The `.jeeby-cms-editor-header a` CSS provides this.

3. Title/slug column div (line 49): Keep `style={{ display: 'flex', flexDirection: 'column', alignItems: 'center', gap: '4px' }}` inline -- this is a one-off layout wrapper for the center section. No class defined for it.

4. Slug row div (line 51): Keep `style={{ display: 'flex', alignItems: 'center', gap: '8px' }}` inline -- one-off layout.

5. H1 title (line 50): Remove `style={{ margin: 0 }}`. The `.jeeby-cms-editor-title` CSS sets margin: 0.

6. Slug input (line 62): Remove `style={{ minHeight: '44px' }}`. The `.jeeby-cms-slug-input` CSS provides min-height: 44px.

7. Publish controls div (line 72-74): Remove `style={{ display: 'flex', alignItems: 'center', gap: '16px' }}`. The `.jeeby-cms-publish-controls` CSS provides this.

8. Retry button (line 97): Remove `style={{ background: 'none', border: 'none', cursor: 'pointer', minHeight: '44px' }}`. Add `className="jeeby-cms-btn-ghost"`.

9. Publish button (line 109): KEEP `style={{ cursor: publishStatus === 'publishing' || saveStatus === 'saving' ? 'not-allowed' : 'pointer', pointerEvents: publishStatus === 'publishing' || saveStatus === 'saving' ? 'none' : undefined }}` -- these are runtime conditionals. Remove `minHeight: '44px'` from it (provided by `.jeeby-cms-btn-primary` CSS).

**src/admin/PageEditor.js** -- Make these exact changes:

1. Loading spinner wrapper (line 203): Keep `style={{ display: 'flex', justifyContent: 'center' }}` -- state-conditional render.

2. Loading spinner div (line 204-206): Remove `style={{ width: '32px', height: '32px', animation: 'jeeby-spin 0.75s linear infinite' }}`. Add `className="jeeby-cms-spinner"`.

3. Editor main div (line 228): Remove `style={{ minHeight: 'calc(100vh - 120px)' }}`. The `.jeeby-cms-editor-main` CSS provides this.

**src/admin/BlockCanvas.js** -- Make these exact changes:

1. Both `.jeeby-cms-block-canvas` divs (lines 92, 103): Remove `style={{ maxWidth: '720px', margin: '0 auto' }}`. The `.jeeby-cms-block-canvas` CSS provides this.

2. Reorder.Group ol element (line 111): Remove `style={{ listStyle: 'none', padding: 0, margin: 0 }}`. The `.jeeby-cms-block-canvas ol` CSS provides this.

3. Reorder.Item li (line 35): KEEP `style={{ listStyle: 'none' }}` -- applied via Framer Motion component prop, separate from CSS context.

4. Drag/delete row div (lines 46-49): KEEP `style={{ display: 'flex', justifyContent: 'space-between', alignItems: 'center', opacity: hovered ? 1 : 0, transition: 'opacity 150ms' }}` -- the opacity is runtime-dynamic. Keep all inline.

5. Drag handle button (lines 54-62): Remove `style={{ cursor: 'grab', touchAction: 'none', background: 'none', border: 'none', minHeight: '44px', minWidth: '44px', display: 'flex', alignItems: 'center', justifyContent: 'center', padding: 0 }}`. Add `className="jeeby-cms-drag-handle"`. The CSS class provides all of this.

6. Delete button (lines 66-72): Remove `style={{ background: 'none', border: 'none', cursor: 'pointer', minHeight: '44px', minWidth: '44px', display: 'flex', alignItems: 'center', justifyContent: 'center', padding: 0 }}`. Add `className="jeeby-cms-btn-ghost"` and add inline `style={{ minWidth: '44px', padding: 0 }}` for the icon button sizing that differs from the standard ghost button.

After all changes in this task, run existing tests to confirm no regressions.
  </action>
  <verify>
    <automated>node --import ./scripts/test-register.js --experimental-test-module-mocks --test 'src/admin/EditorHeader.test.js' 'src/admin/PageEditor.test.js' 'src/admin/BlockCanvas.test.js' 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - src/admin/EditorHeader.js does NOT contain `justifyContent: 'space-between'` on the header element (migrated to CSS)
    - src/admin/EditorHeader.js contains `className="jeeby-cms-btn-ghost"` on the retry button
    - src/admin/PageEditor.js contains `className="jeeby-cms-spinner"` in loading state
    - src/admin/PageEditor.js does NOT contain `minHeight: 'calc(100vh - 120px)'`
    - src/admin/BlockCanvas.js contains `className="jeeby-cms-drag-handle"` on the drag button
    - src/admin/BlockCanvas.js does NOT contain `maxWidth: '720px'`
    - Existing tests pass for all three files
  </acceptance_criteria>
  <done>EditorHeader has only runtime cursor/pointerEvents inline styles. PageEditor has only conditional render display inline styles. BlockCanvas has only runtime opacity/transition and Framer Motion listStyle inline styles. All existing tests pass.</done>
</task>

</tasks>

<verification>
1. `grep -r "@keyframes" src/admin/ --include="*.js"` -- returns 0 results (moved to CSS)
2. `grep -r "'#1f2937'" src/admin/ --include="*.js"` -- returns 0 results (hardcoded hex removed)
3. `grep -r "'#f9fafb'" src/admin/ --include="*.js"` -- returns 0 results
4. `grep -r "'#fff'" src/admin/ --include="*.js"` -- returns 0 results (was in UnsavedChangesWarning, migrated in Plan 03)
5. `node --import ./scripts/test-register.js --experimental-test-module-mocks --test 'src/admin/*.test.js'` -- all tests pass
</verification>

<success_criteria>
- 7 admin component files have visual inline styles migrated to CSS classes
- Accessibility-critical inline styles remain (live regions, runtime conditionals)
- No @keyframes in JSX files
- No hardcoded hex color values in migrated components
- BlockCanvas has new .jeeby-cms-drag-handle class on drag button
- All existing admin tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/08-css-theming/08-02-SUMMARY.md`
</output>
